public class Box {
    public double getVolume(double l, double w, double h) {
        /*if(l > 9 || l < 2 || w > 9 || w < 2 || h > 9 || h < 2)
            throw new InputOutOfRange("Input for all variables must be in between 1-10.");*/
        return l*w*h;
    }
}